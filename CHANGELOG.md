# 📜 QR-Relay 更新日志与版本迭代历史 (Changelog)

本文档记录了 **QR-Relay（临时云盘与二维码快传中转站）** 自项目初生以来的每一次重大版本更新、功能迭代、问题修复与底层架构演进。

---

## 🚀 [v1.6.0] - 2026-09-21

### ✨ 新增与优化
- **中转箱销毁时点精确可视化**：
  - 彻底废除模糊的“定时销毁”纯文本提示，全面升级为**精准时点 + 动态相对倒计时**双重展示。
  - 支持智能自然语言时态：
    - 当天到期：`将于 今日 16:30 销毁 (约45分钟后)`
    - 次日到期：`将于 明日 10:15 销毁 (约18小时后)`
    - 跨周到期：`将于 09-28 14:00 销毁 (7天后)`
    - 即将到期：`即将销毁 (<1分钟后)`
    - 已过期：`已到期 (即将清理)`
  - 鼠标悬浮气泡可查看标准年月日与时分秒精确时间戳。
- **取件提取页销毁时间提示同步**：
  - 在 `share.html` 提取页顶部元数据区，新增带时钟图标的到期销毁指示卡片，让文件接收方也能一目了然得知内容的剩余有效时长。
- **中转箱媒体分类完善**：
  - 中转箱列表与筛选器全面补齐 `视频` 专属紫色图标与标签识别。
- **项目更新日志系统化沉淀**：
  - 新增 `CHANGELOG.md`，并在项目根目录 `README.md` 中建立清晰的迭代跟踪索引。

---

## 🚀 [v1.5.0] - 2026-09-21

### ✨ 新增与优化
- **突破 100MB 限制：Cloudflare R2 预签名直传通道 (最高 5GB)**：
  - 突破 Cloudflare Workers 免费版针对经过 Worker 的 100MB 代理请求体限制。
  - 基于纯原生 JavaScript Web Crypto API（`crypto.subtle`）实现完整的 AWS S3 v4 签名算法，**0 外部 npm 重量级依赖，毫秒级冷启动**。
  - 采用**两阶段直传协议**：
    1. 申请阶段 (`POST /api/upload/direct-request`)：即时触发 10GB 熔断预检与口令防撞，生成 15 分钟有效期的 R2 Presigned PUT URL。
    2. 完工阶段 (`POST /api/upload/direct-complete`)：校验 R2 真实对象存在性，将元数据持久化至 Cloudflare D1 并触发 OpenList 异步归档。
- **前端智能无缝分流传输**：
  - `≤ 100MB` 文件：沿用标准秒传通道。
  - `> 100MB` 超大文件：自动无缝切换至 R2 预签名直传通道，利用原生 `XMLHttpRequest` 精准驱动 0% ~ 100% 实时进度条。
- **底层容错与账号 ID 深度清洗**：
  - 为 Worker 增加容错正则，自动剥离用户误填的 `https://` 协议前缀或 `.r2.cloudflarestorage.com` 终结点后缀，自动还原 32 位十六进制 Account ID。
- **超大文件直传全套技术文档与避坑排查**：
  - 在 `docs/deployment/cloudflare.md` 中补齐 R2 存储桶 CORS 跨域规则配置、R2 API 令牌创建指南及常见错误（网络连接失败、403 Forbidden、CORS error）的排查方法。

---

## 🚀 [v1.4.0] - 2026-09-21

### ✨ 新增与优化
- **10GB 总存储配额安全熔断（$0 永不扣费保护）**：
  - 针对 Cloudflare R2 免费层每月 10GB 存储限制，在服务端（FastAPI 与 Cloudflare Worker）增加代码级强管控。
  - 每次文件写入或直传签发前，实时统计现有所有非文本文件的物理占用 `SUM(file_size)`。
  - 当前文件累加后若超过 10GB，**立即物理熔断拒绝上传（HTTP 413）**，从源头杜绝因超额而产生任何云厂商扣费。
- **报错信息专业化脱敏**：
  - 彻底抹除“免费额度”、“白嫖”等任何敏感字眼。
  - 对外统一输出工业级标准配额报错文案：  
    > *“中转站存储空间不足！上传此文件将超出系统分配的存储配额。请先清理不需要的中转文件或等待到期自动销毁以腾出空间。”*
- **实时前台配额指示看板**：
  - 网站页脚新增中性、专业的存储状态栏：`存储配额: 已用 X MB / 10 GB`。
  - 在文件上传完成或手动删除时，状态栏自动静默刷新。

---

## 🚀 [v1.3.0] - 2026-09-20

### 🐛 缺陷修复与体验升级
- **中文命名视频在线流式播放（iOS / Safari 兼容）**：
  - 全面支持 HTTP 206 Partial Content 与 `Range: bytes=...` 请求探测。
  - 彻底解决移动端 Safari、微信内置浏览器在加载 MP4/MOV 视频时因无法探测 `moov` 头部导致的十字划线播放失败问题。
- **RFC 6266 双模式回落（解决下载文件名丢失后缀）**：
  - 取件下载响应头同时包含 ASCII 兜底文件名与 UTF-8 真实中文编码名：  
    `Content-Disposition: inline; filename="download.mp4"; filename*=utf-8''%E4%B8%AD%E6%96%87.mp4`
  - 彻底根除移动端浏览器把下载文件重命名为无后缀纯数字提取码（如 `851927`）的硬伤。
- **专属深色视频播放器卡片**：
  - 取件页根据 MIME 类型与视频扩展名自动挂载原生流式 `<video>` 播放器，支持全屏播放、画中画与进度条任意拖拽。

---

## 🚀 [v1.2.0] - 2026-09-20

### ✨ 移动端与交互革新
- **原生 App 级沉浸适配与 PWA 独立桌面应用**：
  - 完整支持 PWA 技术规范，在 iOS Safari 或 Android Chrome 中「添加到主屏幕」即可启动独立无边框全屏 Web App。
  - 移动端专享底部毛玻璃悬浮 Dock 栏，单手大拇指丝滑切换。
  - 解决 iPhone 输入框聚焦时页面强制放大的问题（严格适配 16px 视口）。
  - 全面适配全面屏、刘海屏、灵动岛与底部 Home 虚拟手势条安全区。
- **生产部署手册体系化**：
  - 编写多套分级指南：本地局域网指南、Linux VPS Docker 指南、Cloudflare 全托管指南，并附带专供 AI Coding Agent 执行的手册。

---

## 🚀 [v1.1.0] - 2026-09-19

### ☁️ 多网盘生态挂载
- **OpenList / AList 多网盘容灾备份与 WebDAV 自动归档**：
  - 支持网页端在线可视化配置 WebDAV 连接凭据（支持探测挂载路径）。
  - 敏感账密在 SQLite/D1 数据库中加密落盘持久化。
  - 支持文件在中转站生成取件码的同时，后台异步自动推送归档至阿里云盘、夸克网盘、OneDrive、百度网盘或私有 NAS。

---

## 🚀 [v1.0.0] - 2026-09-19

### 🎉 基础版本初生 (Initial Release)
- **双引擎全栈架构**：
  - Python 3.10+ / FastAPI / SQLite 本地与 VPS 容器化架构。
  - Cloudflare Workers / D1 / R2 纯边缘 Serverless 零成本全托管架构。
- **核心快传功能**：
  - 6 位数字取件码 + 离线内容二维码 / 动态取件二维码。
  - 文本、图片、各类文件临时中转与剪贴板极速粘贴。
  - 10分钟、1小时、24小时、7天生命周期管理 (TTL) 与定时自动物理清理。
  - 阅后即焚（首次取件后立即异步清除物理文件与数据库记录）。
  - 访客设备隔离与管理员全站透视管理面板。
